# decorators- whats decorator and whats (*args, **kwargs)
simple example decorate divide function in smart_divide decorator.

     >> divide(4,20)
    2
 
     >> divide(4,0)
    Whoops! cannot divide
